# React weather app

This folder contains the React implementation for the weather forecast app.

## Run locally

```bash
cd react-app
npm install
npm run dev
```

It uses the Open-Meteo geocoding and forecast APIs, requires no API key, and stores saved locations in the browser's `localStorage`.

Build for production with `npm run build`.
